function main() {
    var a, b;  // Declaring both a and b correctly
    var c;
    
    a = 10;
    b = 100;
    
    b = a + b;  // Proper addition of a and b
    c = a * b;  // Assigning the result of a * b to c
    
    document.writeln(c);  // Outputs the value of c
}
